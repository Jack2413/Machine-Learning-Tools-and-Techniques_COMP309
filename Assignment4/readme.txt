The code format is .ipynb, you can use Jupyter Notebook to open it.

-- In part1 and Part2 code, the algorithms has beed commented, if you want to swap algorithms plz uncomment it.

-- In part3 used proived template code if you want change the metric_type and optimizer_type just change the follow line of code.

metric_type = "MAE"  # MSE, RMSE, MAE, R2
optimizer_type = "BGD" # PSO, BGD, MiniBGD

-- Plz use the data I proived, height and weight lable error has been fixed. 
the data path is fixed, so do not change the structure of the file.